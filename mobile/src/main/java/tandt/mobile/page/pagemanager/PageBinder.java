package tandt.mobile.page.pagemanager;

import com.google.inject.AbstractModule;

public abstract class PageBinder<TBinder extends PageBinder> extends AbstractModule {

    @Override
    protected void configure() {
        super.configure();
    }
}
